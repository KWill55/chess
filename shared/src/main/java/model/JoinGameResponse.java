package model;

/**
 * Represents a response for a successful game join request.
 * This response does not contain any data, as a successful join operation
 * is indicated by an HTTP 200 status code.
 */
public record JoinGameResponse() {}
